{
  "name": "hue-theme",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "packageManager": "bun@1.2.20",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "bun run --cwd packages/tokens build && bun run --cwd apps/gallery build",
    "dev": "bun run --cwd packages/tokens build && bun run --cwd apps/gallery dev",
    "test": "bun test",
    "check": "bun run --cwd packages/tokens check && bun run --cwd apps/gallery check",
    "qa:visual": "node scripts/visual-qa.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "playwright": "^1.61.1",
    "typescript": "^5.8.3"
  }
}
